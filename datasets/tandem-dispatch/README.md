# Tandem Dispatch

Lifecycle records for Codex-to-Zo tandem dispatches.

This dataset tracks the machine work loop:

```text
created -> shipped -> triggered -> running -> talkback_ready -> fetched -> ratified
```

Default local report path:

```text
datasets/tandem-dispatch/reports/
```

Use this dataset to inspect tandem work without reading live Zo user datasets.
