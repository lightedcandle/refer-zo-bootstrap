# Law 52: refer.repair.md - Compatibility Alias

`refer.repair` remains an accepted legacy alias for `refer.combing`.

Canonical doctrine now lives in:

- `REFER.OS/refer.combing.md`

Alias behavior:

- Requests phrased as `refer.repair` must route to the combing doctrine.
- Existing references to repair remain valid for backward compatibility.
- New governance, routing, and operational language should prefer `refer.combing`, `combing`, and `alignment`.

Compatibility rule:

- `refer.repair` and `refer.combing` are interpreted as the same execution family.
- When wording matters, `refer.combing` is authoritative and `refer.repair` is a legacy entry token.
